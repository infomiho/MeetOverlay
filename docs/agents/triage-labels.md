# Triage Labels

This repo does not use issue labels for local markdown issues.

When a skill needs a triage role, record the role as plain text in the issue file instead of applying a tracker label.
